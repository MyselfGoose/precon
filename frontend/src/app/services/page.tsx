import { SERVICES } from '@/lib/data'; import { CTA, PageHead, ServiceCard } from '../components';
export default function ServicesPage(){return <><PageHead eyebrow="Services" title="Estimating, takeoffs and bid support" lede="Five services that cover a project from conceptual budget through bid day. Take one or take all of them."/><section className="band"><div className="wrap stack-lg"><div className="grid-2">{SERVICES.map(s=><ServiceCard service={s} full key={s.slug}/>)}</div><div className="note"><b>Not sure which you need?</b> Most contractors start with a single-trade takeoff on one live bid to see the deliverable before committing to anything larger.</div></div></section><CTA title="Start with one bid" text="Send a live set and see what comes back before you commit to anything ongoing." secondary={false}/></>}
