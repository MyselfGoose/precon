import { CONTENT_SERVICES } from '@/lib/content';
import { CTA, PageHead, ServiceSummaryCard } from '../components';
import { createMetadata } from '@/lib/metadata';
export const metadata = createMetadata({ title: 'Services', description: 'Explore estimating, drafting, engineering, visualization, and acquisition support from PreCon Ext.', path: '/services' });
export default function ServicesPage(){return <><PageHead eyebrow="Services" title="Support for the decisions before construction" lede="Choose the work product you need: a defensible estimate, clearer drawings, coordinated engineering, useful visualization, or property analysis grounded in scope and cost."/><section className="band"><div className="wrap stack-lg"><div className="stack"><div className="eyebrow">Choose by decision</div><p className="prose">Every service page explains the practical output, included work, workflow, and the information that affects scope. If your project crosses disciplines, send the full brief and we will help assemble the right combination.</p></div><div className="grid-2">{CONTENT_SERVICES.map(s=><ServiceSummaryCard service={s} key={s.slug}/>)}</div></div></section><CTA title="Not sure which service fits?" text="Send the project information you have. We will help define the useful deliverable, the open questions, and the next conversation." /></>}
