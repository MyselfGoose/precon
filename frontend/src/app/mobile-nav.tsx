'use client';
import { useEffect, useRef, useState } from 'react';
import Link from 'next/link';
import { NAV_ITEMS } from '@/lib/data';
import { SITE_COPY } from '@/lib/content';
export default function MobileNav(){const [open,setOpen]=useState(false);const closeRef=useRef<HTMLButtonElement>(null);const toggleRef=useRef<HTMLButtonElement>(null);useEffect(()=>{if(!open)return;closeRef.current?.focus();const onKeyDown=(event:KeyboardEvent)=>{if(event.key==='Escape'){setOpen(false);toggleRef.current?.focus();}};document.addEventListener('keydown',onKeyDown);return()=>document.removeEventListener('keydown',onKeyDown);},[open]);return <><button ref={toggleRef} className="nav-toggle" aria-expanded={open} aria-controls="mobile-links" aria-label={open?'Close navigation':'Open navigation'} onClick={()=>setOpen(!open)}>{open?'Close':'Menu'}</button>{open&&<nav id="mobile-links" className="mobile-links" aria-label="Mobile navigation"><button ref={closeRef} className="mobile-close" type="button" onClick={()=>{setOpen(false);toggleRef.current?.focus();}}>Close menu</button>{NAV_ITEMS.map(([n,h])=><Link key={h} href={h} onClick={()=>setOpen(false)}>{n}</Link>)}<Link className="btn btn-primary btn-sm" href="/quote" onClick={()=>setOpen(false)}>{SITE_COPY.cta.primary}</Link></nav>}</>}
