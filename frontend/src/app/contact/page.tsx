import { CTA, PageHead, Svg } from '../components'; import { D } from '@/lib/illustrations'; import Link from 'next/link';
export default function Contact(){return <><PageHead eyebrow="Contact" title="Talk to an estimator" lede="Estimators answer the phone directly. For a quote on a specific set, the request form is faster — it asks for everything we need."/><section className="band"><div className="wrap contact-grid"><div className="stack-lg"><div className="grid-2">{[['Phone','(800) 555-0100','[Business hours, time zone]'],['Email','[email@placeholder.com]','Plan sets over 25 MB — send a link'],['Office','[Company Name]','[Street address] · [City, State ZIP]'],['Rush bids','Call, don’t email','Under 48 hours to close — schedule by phone']].map(([k,v,h])=><div className="contact-block" key={k}><span className="k">{k}</span>{k==='Phone'?<a className="v" href="tel:+18005550100">{v}</a>:<span className="v">{v}</span>}<span className="hint">{h}</span></div>)}</div><div className="note"><b>Have a set ready?</b> Use the <Link href="/quote">quote request form</Link> — it captures the bid date, trades and location.</div></div><div className="draw"><Svg markup={D.exterior()}/><div className="draw-cap"><b>Nationwide</b><span>Contractors in all fifty states</span></div></div></div></section><CTA title="Ready to send the set?" text="Request a quote and we will get back to you with a clear next step."/></>}
