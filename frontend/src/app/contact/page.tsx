import { CTA, PageHead, Svg } from '../components'; import { D } from '@/lib/illustrations'; import Link from 'next/link'; import { BRAND } from '@/lib/content';
export default function Contact(){return <><PageHead eyebrow="Contact PreCon Ext" title="Bring the next decision into focus" lede="Share the project information you have. We will help identify the right service, scope, and next step."/><section className="band"><div className="wrap contact-grid"><div className="stack-lg"><div className="grid-2">{[['Phone',BRAND.phone,'Contact details and hours pending confirmation'],['Email',BRAND.email,'Project files can be shared by link'],['Office','Remote project support','Serving project teams across the United States'],['Project discussion','Start with the quote form','Tell us the role, project type, scope, and timing']].map(([k,v,h])=><div className="contact-block" key={k}><span className="k">{k}</span>{k==='Phone'?<a className="v" href="tel:+18005550100">{v}</a>:<span className="v">{v}</span>}<span className="hint">{h}</span></div>)}</div><div className="note"><b>Have project information ready?</b> Use the <Link href="/quote">project request form</Link> to capture the scope, service, and timing in one place.</div></div><div className="draw"><Svg markup={D.exterior()}/><div className="draw-cap"><b>PreCon Ext</b><span>Integrated support from concept to construction</span></div></div></div></section><CTA title="Start a project conversation" text="Request a clear next step from the PreCon Ext team."/></>}
