import { CTA, PageHead, Svg } from '../components'; import { D } from '@/lib/illustrations'; import Link from 'next/link'; import { BRAND } from '@/lib/content';
export default function Contact(){return <><PageHead eyebrow="Contact PreCon Ext" title="Bring the next decision into focus" lede="Share the project information you have. We will help identify the right service, scope, and next step."/><section className="band"><div className="wrap contact-grid"><div className="stack-lg"><div className="grid-2">{[['Phone',BRAND.phone,'Call when the scope is easier to explain out loud'],['Project request','Send your plans','Use the request form to organize files, timing, and priorities'],['Office','Remote project support','Serving project teams across the United States'],['Project discussion','Talk through your scope','Tell us the role, project type, scope, and timing']].map(([k,v,h])=><div className="contact-block" key={k}><span className="k">{k}</span>{k==='Phone'?<a className="v" href="tel:+18005550100">{v}</a>:k==='Project request'?<Link className="v" href="/quote">{v}</Link>:<span className="v">{v}</span>}<span className="hint">{h}</span></div>)}</div><div className="note"><b>Have project information ready?</b> Use the <Link href="/quote">project request form</Link> to capture the scope, service, and timing in one place.</div></div><div className="draw"><Svg markup={D.exterior()}/><div className="draw-cap"><b>PreCon Ext</b><span>Integrated support from concept to construction</span></div></div></div></section><CTA title="Ready to make the next decision?" text="Send your plans and start with a clearer project conversation."/></>}
