import { TRADES } from '@/lib/data'; import { CTA, PageHead, TradeTile } from '../components';
import { createMetadata } from '@/lib/metadata';
export const metadata = createMetadata({ title: 'Trades', description: 'Review trade-by-trade estimating and quantity takeoff support organized by CSI division.', path: '/trades' });
export default function TradesPage(){return <><PageHead eyebrow="Trades" title="Find the estimating scope that fits the set" lede="Each trade page shows the work measured, the units reported, the sheets typically used, a sample output, exclusions, and questions to resolve before pricing."/><section className="band"><div className="wrap stack-lg"><div className="trades">{TRADES.map(t=><TradeTile trade={t} key={t.slug}/>)}</div><div className="note"><b>Working across divisions?</b> Full-building estimates combine the selected trades into one workbook with a summary tab, division tabs, shared assumptions, and a single exclusions list.</div></div></section><CTA title="Send the set for one trade or the full building" text="Tell us which divisions matter, what stage the project is in, and when the bid or decision is due." /></>}
