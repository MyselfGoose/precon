import { TRADES } from '@/lib/data'; import { CTA, PageHead, TradeTile } from '../components';
import { createMetadata } from '@/lib/metadata';
export const metadata = createMetadata({ title: 'Trades', description: 'Review trade-by-trade estimating and quantity takeoff support organized by CSI division.', path: '/trades' });
export default function TradesPage(){return <><PageHead eyebrow="Trades" title="Estimating by division" lede="Twelve CSI MasterFormat divisions. Each page covers what we measure, the units we report in, a sample of the deliverable, and what we exclude by default."/><section className="band"><div className="wrap stack-lg"><div className="trades">{TRADES.map(t=><TradeTile trade={t} key={t.slug}/>)}</div><div className="note"><b>Working across divisions?</b> Full-building estimates combine any of these into one workbook with a summary tab, division tabs and a single exclusions list.</div></div></section><CTA title="Send the set for any trade — or all of them" text="Tell us which divisions you need and when the bid is due."/></>}
