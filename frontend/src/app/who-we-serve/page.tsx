import { AUDIENCE_CONTENT } from '@/lib/content';
import { CTA, PageHead, Spec } from '../components';
import { createMetadata } from '@/lib/metadata';
export const metadata = createMetadata({ title: 'Who We Serve', description: 'PreCon Ext supports contractors, specialty teams, owners, developers, and investors with practical project intelligence.', path: '/who-we-serve' });
import { ICO } from '@/lib/illustrations';
export default function Who(){return <><PageHead eyebrow="Who we serve" title="Find support shaped around your project role" lede="The useful deliverable changes depending on whether you are bidding work, protecting a trade margin, planning an acquisition, or coordinating a project team. Start with the pressure you need to resolve."/><section className="band"><div className="wrap stack-lg">{AUDIENCE_CONTENT.map(a=><div className="split" style={{alignItems:'start'}} key={a.code}><div className="stack"><div className="ico" style={{width:56,height:56}} dangerouslySetInnerHTML={{__html:ICO[a.icon as keyof typeof ICO]}}/><div className="code">{a.code}</div><h2 style={{fontSize:'var(--s2)'}}>{a.name}</h2><p className="prose">{a.intro}</p><p className="prose">{a.details}</p></div><Spec title="Typical support" unit={a.code} items={a.items}/></div>)}</div></section><CTA title="Tell us what your team needs to decide" text="Share your role, project stage, scope, and timing so the first conversation starts in the right place." /></>}
