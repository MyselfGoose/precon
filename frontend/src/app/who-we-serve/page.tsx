import { AUDIENCES } from '@/lib/data'; import { CTA, PageHead, Spec } from '../components'; import { ICO } from '@/lib/illustrations';
export default function Who(){return <><PageHead eyebrow="Who we serve" title="Different buyers, different problems" lede="A general contractor, a trade subcontractor and a material supplier all need quantities — but they need them organized, priced and delivered differently. We build the estimate to match."/><section className="band"><div className="wrap stack-lg">{AUDIENCES.map(a=><div className="split" style={{alignItems:'start'}} key={a.slug}><div className="stack"><div className="ico" style={{width:56,height:56}} dangerouslySetInnerHTML={{__html:ICO[a.icon as keyof typeof ICO]}}/><h2 style={{fontSize:'var(--s2)'}}>{a.name}</h2><p className="prose">{a.intro}</p><p className="prose">{a.details}</p></div><Spec title="Typical engagements" unit={a.code} items={a.items}/></div>)}</div></section><CTA title="Tell us which one you are" text="The quote form asks — and the estimate is built accordingly."/></>}
