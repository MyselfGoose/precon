import { AUDIENCE_CONTENT } from '@/lib/content';
import { CTA, PageHead, Spec } from '../components';
export default function Who(){return <><PageHead eyebrow="Who we serve" title="Support shaped around the team doing the work" lede="Every project has different pressures. PreCon Ext gives contractors, specialty teams, owners, developers, and investors the information they need to make the next decision with confidence."/><section className="band"><div className="wrap stack-lg">{AUDIENCE_CONTENT.map(a=><div className="split" style={{alignItems:'start'}} key={a.code}><div className="stack"><div className="code">{a.code}</div><h2 style={{fontSize:'var(--s2)'}}>{a.name}</h2><p className="prose">{a.intro}</p><p className="prose">{a.details}</p></div><Spec title="Typical support" unit={a.code} items={a.items}/></div>)}</div></section><CTA title="Tell us where you fit" text="The right scope starts with understanding your role, project, and next decision." /></>}
